# Project Intelligence

## 项目概况

- 项目当前是为“运满物流”创建的宣传官网。
- 仓库初始只有 `resources/` 图片资源，没有既有前端框架。
- 当前技术栈为 Vite + React，面向组件化维护和静态部署。

## 工作偏好

- 回复用户始终使用中文。
- 设计实现应以美观、大气、专业可信为目标。
- 优先复用用户放在 `resources/` 中的资源。

## 技术注意事项

- 使用 `npm run dev` 启动本地预览。
- 使用 `npm run build` 验证生产构建。
- 联系表单当前仅做前端演示，正式上线可接入表单服务或后端 API。
